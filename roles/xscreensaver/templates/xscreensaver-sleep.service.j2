[Unit]
Description=Ensure XScreenSaver locks {{ name }} sleep
{{ option }}=sleep.target

[Service]
User=%I
Type=forking
ExecStart=/usr/bin/xscreensaver-command -lock
ExecStartPost=/usr/bin/sleep 1

[Install]
WantedBy=sleep.target

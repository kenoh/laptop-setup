---
- hosts: localhost
  become_user: root
  vars:
    install_packages:
      - ack
      - acpi
      - arandr
      - autoconf
      - automake
      - colordiff
      - ctags
      - emacs
      - feh
      - firefox
      - gdb
      - gimp
      - git
      - global-ctags
      - htop
      - mc
      - mercurial
      - nmap
      - python2-ipython
      - python3-ipython
      - screen
      - strace
      - tilix
      - tig
      - tmux
      - vim
      - wireshark
      - python-xdot
      - yum-utils
  roles:
    - install-packages
    - i3-desktop
    - google-chrome
    - user-groups
    - vagrant
    - docker
    - emacs-server
    - fedora-codecs
    - spotify

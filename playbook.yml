---
- hosts: localhost
  become_user: root
  vars:
    install_packages:
      - vim
      - emacs
      - i3
      - dunst
      - compton
      - git
      - tig
      - mc
      - nmap
      - wireshark
  roles:
    - install-packages
    - google-chrome
